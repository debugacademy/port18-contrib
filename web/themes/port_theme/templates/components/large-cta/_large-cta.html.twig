<div class="large-cta">
    <img src="{{ image_url }}" class="background-image" alt="{{ image_alt_text }}"/>
    <div class="overlay-content">
        <img src="{{ icon_url }}" class="icon"/>
        <h2>{{ heading_line_1 }} 
            <span class="heading-line-2">{{ heading_line_2 }}</span>
        </h2>
        <div class="body-text">{{ body_text|raw }}</div>
        <a href="{{ Link_url }}" class="button">{{ link_text }}</a>
    </div>
</div>